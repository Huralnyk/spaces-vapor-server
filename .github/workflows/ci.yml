name: CI

on: [push]

jobs:
  build:

    runs-on: macOS-latest

    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: 
        brew install openssl
        swift build -v
    - name: Run tests
      run: swift test -v
